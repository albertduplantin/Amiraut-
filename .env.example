DATABASE_URL="postgresql://user:password@host:5432/dbname"
AUTH_SECRET="generate-with-openssl-rand-base64-32"

# Optionnel : emails (confirmation, mot de passe oublié, rappels)
# Sans ces variables, l'app fonctionne normalement mais n'envoie aucun email.
RESEND_API_KEY=""
RESEND_FROM_EMAIL="Les Semaines de l'Hexagone <noreply@votre-domaine.fr>"
NEXT_PUBLIC_SITE_URL="https://amiraut.vercel.app"

# Optionnel : sécurise la route /api/cron/rappel (auto-fourni par Vercel Cron)
CRON_SECRET="generate-with-openssl-rand-hex-24"
